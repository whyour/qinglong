apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: ql3-provider-credential-management-client
  namespace: qinglong3-system
  labels:
    app.kubernetes.io/name: ql3-provider-credential-management-client
    app.kubernetes.io/component: provider-credential-management-client
    app.kubernetes.io/part-of: qinglong3
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: ql3-provider-credential-management-client
      app.kubernetes.io/component: provider-credential-management-client
  policyTypes:
    - Ingress
    - Egress
  ingress: []
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: ql3-provider-credential-management
              app.kubernetes.io/component: provider-credential-management
      ports:
        - protocol: TCP
          port: 8446
